{
  "name": "transylvania-web",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-data": "node ../tools/sync-data.mjs",
    "predev": "npm run sync-data",
    "dev": "vite --port 8099 --host --strictPort",
    "prebuild": "npm run sync-data",
    "build": "vite build",
    "preview": "vite preview --port 8099",
    "pretest": "npm run sync-data",
    "test": "node --test 'test/*.test.js'"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
